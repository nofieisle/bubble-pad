{
  "name": "bubble-pad",
  "version": "1.0.0",
  "private": true,
  "description": "A Chrome extension that provides a floating text area for drafting messages",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.3",
    "vite": "^6.2.2"
  }
}
